# Change Request

- **CR ID**: CR-2026-07-30-003
- **Status**: Closed
- **Linked Active Spec**: 005-quality-gaps
- **CR Relation**: impacts
- **Target Baseline**: Completed spec
- **Target Identifier**: 005-quality-gaps
- **Summary**: Remove the remaining React render-time ref access warning in `SplitButton`.
- **Requested Delta**: Pass MUI Popper a lazy anchor callback instead of reading `anchorRef.current` while React renders.
- **Why Now**: Every local and hosted lint run reports `react-hooks/refs`; render-time ref reads can observe stale mutable state and do not participate in React updates.
- **Resolution Notes**: Use the existing MUI `anchorEl` callback contract. No state, dependency, public API, or interaction behavior changes.

## Adaptive Execution Plan

- **Execution Profile**: `default`
- **Adaptive Depth**: `compact`
- **Signals**: single-file React correctness warning; library-supported one-line fix; committed embedded bundle requires rebuild
- **Mandatory Gates**: requirements and design readback; TDD/test evidence; security gate; formal review; registry freshness; worktree cleanup
- **User Override**: the active goal authorizes progressing high-value bugs through dev and main

| Activity | Disposition | Rationale | Evidence Target |
|---|---|---|---|
| Requirements reconciliation | run | The change must strengthen REQ-QG-002 without changing accepted UI behavior | SPEC-005 `requirements.md`, `review.md` |
| Design and task consistency | run | Reuse MUI's existing callback contract rather than add state or abstraction | source diff and type-check |
| TDD and registered tests | run | Prove warning removal and preserve unit/build/E2E behavior | `yarn lint`, `yarn test`, `yarn build`, hosted E2E |
| Security gate | run | Preserve the correlated supply-chain baseline after rebuilding the bundle | `make security`, hosted CodeQL |
| Registry freshness | run | Reconcile the historical documented warning and test evidence | SPEC-005 review, `ui/TESTS.md`, RTM, ISSUE_LOG, NEXT_STEPS |
| Formal review | run | Verify the lazy callback cannot change selection or click-away semantics | CR review report |
| Worktree recovery and cleanup | run | Merge through dev/main, then remove the isolated lane | final branch/worktree inventory |

## Re-sync Freshness Evidence

- **Checked At**: 2026-07-30
- **Valid Until**: until `SplitButton`, MUI Popper, or the lint configuration changes
- **SPECS.md Read**: `.agents/specs/SPECS.md`
- **Upstream Source Checked**: SPEC-005 requirements/design/review; current `SplitButton.tsx`; local CI warning
- **Observed Pin/Version**: `origin/main@cb680587fd5d2bf61d217e89e3fb91bca40f42f4`
- **Outcome**: ok

## External Contract Only

- **Source of Truth**: MUI Popper `anchorEl` callback type in the installed dependency
- **Pin/Version**: `@mui/material` 5.18.0
- **Assumption Snapshot**: callback is invoked only after the mounted button group opens the Popper
- **Observed Constraint / Behavior**: MUI 5.18 requires the callback to return a non-null `HTMLElement`; the mounted/open invariant is expressed with a callback-local non-null assertion
