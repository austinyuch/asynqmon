# Change Request

- **CR ID**: CR-2026-07-30-004
- **Status**: Closed
- **Linked Active Spec**: 006-local-supply-chain-security
- **CR Relation**: impacts
- **Target Baseline**: Completed spec
- **Target Identifier**: 006-local-supply-chain-security
- **Summary**: Prevent generated JavaScript dependencies from entering the Go build/vet/test/govulncheck package scope.
- **Requested Delta**: Discover module-owned Go packages once through a repo-owned allowlist policy and exclude any package under `node_modules`.
- **Why Now**: Warm pre-push runs list `ui/node_modules/flatted/golang/pkg/flatted` as a Go test target, proving that `./...` changes meaning after UI installation.
- **Resolution Notes**: Both canonical local CI and the security runner consume the same package list. A fixture test proves module root retention and `node_modules` exclusion.

## Adaptive Execution Plan

- **Execution Profile**: `harden`
- **Adaptive Depth**: `standard`
- **Signals**: security gate scope drift; generated dependency content; shared build/vet/test/govulncheck boundary; shell runner change
- **Mandatory Gates**: requirements/design readback; TDD fixture; security review; full local CI; formal review; registry freshness; worktree cleanup
- **User Override**: active goal authorizes security fixes through dev/main

| Activity | Disposition | Rationale | Evidence Target |
|---|---|---|---|
| Requirements reconciliation | run | REQ-LSS-003 requires a stable canonical gate scope | SPEC-006 requirements/review |
| Design and task consistency | run | One shared discovery script prevents build and govulncheck drift | runner diff and package-list evidence |
| TDD and registered tests | run | A generated fixture must fail into scope before policy and be absent after policy | `scripts/test-go-owned-packages.sh`, `scripts/TESTS.md` |
| Security gate | run | Package exclusion changes SCA execution scope and must remain fail-closed | `make security`, CodeQL, security review |
| Registry freshness | run | Update folder test authority, workspace rollup, RTM, issue history, and memo | TESTS/RTM/ISSUE_LOG/NEXT_STEPS |
| Formal review | run | Check shell quoting, empty-list behavior, and exclusion boundary | deterministic review report |
| Recovery and worktree cleanup | run | Merge through dev/main and remove only this lane/cache | final topology inventory |

## Re-sync Freshness Evidence

- **Checked At**: 2026-07-30
- **Valid Until**: until Go package layout or local CI runner changes
- **SPECS.md Read**: `.agents/specs/SPECS.md`
- **Upstream Source Checked**: SPEC-006 requirements/review; `scripts/local-ci.sh`; `scripts/security-local.sh`; warm pre-push output
- **Observed Pin/Version**: `origin/main@77158f12cedf60862f69418beca9c8c941eaa24f`
- **Outcome**: ok

## External Contract Only

- **Source of Truth**: Go `list ./...` package discovery and Bash array argument semantics
- **Pin/Version**: Go 1.26.5; Bash runner
- **Assumption Snapshot**: `go list` emits one import path per line; package paths cannot contain newlines
- **Observed Constraint / Behavior**: directories named `node_modules` are not automatically excluded by Go recursive package discovery; discovery disables VCS stamping because it emits names only, while actual builds retain default stamping
