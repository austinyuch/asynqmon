#!/usr/bin/env bash
# pre-push: run the canonical repo-local CI, including SBOM/CVE/KEV gates.
# Enable with: git config core.hooksPath githooks
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
"${repo_root}/scripts/local-ci.sh" --pre-push
